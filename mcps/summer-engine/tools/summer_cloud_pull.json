{
  "name": "summer_cloud_pull",
  "description": "Pull Summer Cloud changes into the local project.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "project": {
        "type": "string",
        "description": "Project root path. Defaults to the current working directory."
      },
      "bootstrap": {
        "type": "string",
        "enum": [
          "keep-cloud",
          "keep-local",
          "merge"
        ],
        "description": "No-base bootstrap choice."
      },
      "adoptPath": {
        "type": "boolean",
        "description": "Accept that the project folder moved and update the recorded path."
      }
    },
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}